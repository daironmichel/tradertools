# source: http://localhost:5000/api/gql/
# timestamp: Wed Nov 20 2019 17:40:57 GMT-0500 (Eastern Standard Time)

interface DatabaseId {
  databaseId: Int
}

"""An object with an ID"""
interface Node {
  """The ID of the object."""
  id: ID!
}

type Query {
  viewer: ViewerType

  """The ID of the object"""
  riskStrategy(id: ID!): RiskStrategyNode
}

type RiskStrategyNode implements Node & DatabaseId {
  """The ID of the object."""
  id: ID!
  name: String!
  exposurePercent: Int!
  profitPercent: Int!
  lossPercent: Int!
  feePerTrade: Float!
  databaseId: Int
}

type ViewerCredentialsType {
  databaseId: Int
  fullName: String
}

type ViewerType {
  credentials: ViewerCredentialsType
  riskStrategies: RiskStrategyNode
}
