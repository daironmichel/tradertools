import React from 'react';
// import styled from '@emotion/styled';
import { Classes } from '@blueprintjs/core';

const At = (): JSX.Element => (
  <span className={Classes.TEXT_MUTED} css={{ opacity: 0.7 }}>
    @
  </span>
);
export default At;
