{
  "schemaPath": "server/schema.graphql",
  "extensions": {
    "endpoints": {
      "default": {
        "url": "${env:BACKBONE_API}/api/gql/",
        "headers": {
          "Authorization": "Token ${env:BACKBONE_API_TOKEN}"
        }
      }
    }
  }
}