{
  "schemaPath": "relay/schema.graphql",
  "extensions": {
    "endpoints": {
      "default": {
        "url": "${env:BACKBONE_API}/api/gql/",
        "headers": {
          "Authorization": "Token ${env:GRAPHQL_CLI_TOKEN}"
        }
      }
    }
  }
}
